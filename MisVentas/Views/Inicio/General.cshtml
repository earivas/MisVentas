@model MisVentas.Models.BI_Presupuestos
@{
    ViewBag.Title = "General";
}

<h2>General</h2>

@*@model List<TableInfo>

if (Model.Count > 0)
{
<table>
    <tr>
        <td>Mes</td>
        <td>Anterior</td>
        <td>Actual</td>
        <td>Presupuesto</td>
    </tr>
    @foreach (TableInfo item in Model)
    {
        <tr>
            <td>@item.MesNombre</td>
            <td>@item.VentaMLAnterior</td>
            <td>@item.VentaMLActual</td>
            <td>@item.PptoMlocal</td>
        </tr>
    }
</table>
}*@